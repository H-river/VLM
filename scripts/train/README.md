# Training entry points

Canonical numerical training remains `python -m continuous_control_v12.train_forward_model`.
Qwen QLoRA engineering entry points remain under `qwen_vl_supervisor_v1/`; they
are GPU-oriented and do not constitute a completed frozen evaluation.

