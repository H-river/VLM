# Evaluation entry points

Use `optics-vla all-smoke`, `python scripts/reproduce/verify_published_table.py`,
and the commands indexed in `docs/EXPERIMENT_STATUS.md`. Historical and
candidate evaluators remain beside their source packages to preserve hashes.

