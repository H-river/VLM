# Documentation archive index

Historical compact reports remain under `legacy/experiments/` and earlier
cleanup records under `legacy/cleanup_reports/`. They are intentionally linked
from the audit instead of copied here, which avoids creating another ambiguous
version of the same result.

