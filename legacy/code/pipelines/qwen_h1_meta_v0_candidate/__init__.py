"""Candidate-only Qwen H1 meta-controller.

This package is additive.  It does not register itself in the existing v12
runtime, does not expose a continuous-action model output, and keeps the
feature flag off by default.
"""

__version__ = "0.1.0-candidate"
