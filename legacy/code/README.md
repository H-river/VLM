# Archived code lines

These packages are preserved for reproducibility, but they are not part of the
current plan-reasoning controller path.

- `rebuilds/`: versioned specialist, measurement, direction, forward, TABM,
  physics, and controller rebuilds superseded by the active stack.
- `pipelines/`: earlier end-to-end, SFT, orchestration, supervisor, and
  profile-to-setup experiments.

The active stack intentionally remains at repository root so its historical
absolute imports continue to work.
